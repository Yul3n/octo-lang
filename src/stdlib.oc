-- Booleans

type bool =
  True | False

iszero 0 = True
iszero n = False

fact 0 = 1
fact n = n * (fact (n - 1))

if True x _  = x
if False _ x = x

ifz x y z =
  if (iszero x) y z

-- Combinators

id x =
  x

cons x _ =
  x

compose f g x =
  f (g x)

flip f g x =
  g (f x)
